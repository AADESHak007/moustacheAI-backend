# =============================================================================
# AI Mustache Generator — Backend Dependencies
# Python 3.11+
# =============================================================================

# Web Framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# AI — Google Gemini
google-generativeai==0.7.2

# Image Processing (resize + JPEG encode only — no OpenCV/MediaPipe needed)
Pillow==10.3.0

# Supabase (Auth + DB + Storage)
supabase==2.4.6      # bundles gotrue, storage3, postgrest

# Email validation (required by Pydantic EmailStr in auth models)
email-validator==2.1.1

# Rate Limiting
slowapi==0.1.9

# Payments — Razorpay (prepaid credit packs)
razorpay==1.4.2

# Config & Utilities
pydantic-settings==2.2.1
python-dotenv==1.0.1
httpx==0.27.0
aiofiles==23.2.1

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
